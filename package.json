{
  "name": "backendtutorial2",
  "version": "1.0.0",
  "description": "full stack application that uses a NodeJS backend, a SQLite database and JWT authentication.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --env-file=.env src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  }
}
